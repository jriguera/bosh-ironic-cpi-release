#!/usr/bin/env bash

PACKAGE="openssl"
VERSION="1.0.2d"
FILE="openssl-${VERSION}.tar.gz"
URL="http://openssl.org/source/openssl-1.0.2d.tar.gz"

if [ ! -s "${PACKAGE}/${FILE}" ]; then
  echo "Downloading ${URL} ..."
  mkdir -p ${PACKAGE}
  curl -s "${URL}" -o "${PACKAGE}/${FILE}"
fi
